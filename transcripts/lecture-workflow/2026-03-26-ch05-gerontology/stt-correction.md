# STT Correction

## Correction summary

- Raw STT is preserved at `transcripts/class-stt/2026-03-26-ch05-gerontology-class-stt.txt`.
- This pass fixes recognizer errors that would distort later evidence extraction.
- The opening attendance / admin chatter was not fully normalized because it is not used for reading evidence.
- This is an analysis-grade correction pass grounded in the raw STT plus the chapter terminology from `ch05-gerontology`; it is not a brand-new audio re-transcription.

## Notable STT fixes

- `랜수면` was normalized to `REM 수면` around raw line `137`.
- `test 테론` was normalized to `테스토스테론(testosterone)` around raw line `80`.
- `터미널 decline`, `time to death`, `distance to death`, `frailty`, `life satisfaction` were normalized around raw lines `44`, `269`, and `272`.
- `텔로미어`, `Hayflick limit`, and related cell-replication terminology were normalized around raw lines `101` to `122`.
- `Hallmarks of Aging`, `disabled macroautophagy`, `dysbiosis`, and `chronic inflammation` were normalized around raw lines `317` to `323`.
- `데이비드 싱클레어`, `Altos Labs`, `제프 베조스`, `유리 밀너`, `존 거든`, and `야마나카 신야` were normalized around raw lines `356` to `410`.
- Obvious word-fragment or homophone errors that changed meaning were corrected, but uncertain student names were left out when they were not content-relevant.

## Corrected transcript

- `03:50` 화자 1: 오늘은 `신체적 노화` 파트 첫 번째다. 이 파트는 내 전공 한가운데는 아니지만, 노년학에서 빼놓을 수 없는 내용이고 흥미로운 포인트가 많다.
- `05:23` 화자 4: 인상 깊었던 부분은 노화를 `1차적 노화`, `2차적 노화`, `3차적 노화`로 나누어 설명한 점이다. 1차적 노화는 누구나 겪는 보편적 변화이고, 2차적 노화는 대체로 겪지만 예방이나 지연 가능성이 있으며, 3차적 노화는 생애 말기에 급격하게 나타나는 쇠퇴로 이해했다.
- `06:10` 화자 1: 어제 장례식장에서 들은 이야기가 바로 이 구분과 연결됐다. 집에서 돌아가신 어르신의 마지막 며칠 동안 기능이 급속히 떨어졌다는 이야기였고, 이런 급격한 하강을 영어로 `terminal decline`이라고 부른다.
- `07:45` 화자 4: 생물학적 노화 이론 중에서는 `면역이론`이 가장 인상적이었다.
- `08:02` 화자 1: 노화 이론이 여러 개인 이유는 노화를 하나로 완전히 설명하지 못하기 때문이다. 이론이 많다는 사실 자체가 아직 단일한 설명으로 닫히지 않았다는 뜻이다.
- `08:54` 화자 1: 감각기관 변화, 특히 `청각` 변화는 실제 일상에서 굉장히 중요하다. 어르신의 큰 목소리나 반응을 단순히 성격 문제로 오해하지 말고 감각 저하의 가능성을 봐야 한다.
- `09:38` 화자 1: 남성의 청각 문제뿐 아니라 방광 기능 저하처럼 일상 행동을 바꾸는 신체 변화들도 많다. 겉으로는 짜증이나 고집처럼 보일 수 있지만, 생리적 이유가 있을 수 있다.
- `10:27` 화자 1: 태양 노출 때문에 얼굴 한쪽이 더 심하게 늙은 화물차 기사 사진은 유명한 사례다. 이런 경우는 보편적 1차적 노화라기보다 환경이 강하게 개입한 `2차적 노화`를 잘 보여준다.
- `12:05` 화자 6: 머리카락 변화도 인상적이었다. 책에서 남성은 `테스토스테론(testosterone)`의 작용 때문에 탈모가 나타날 수 있다고 설명한다.
- `12:57` 화자 1: 하지만 머리숱과 머릿결 문제는 남성만의 이슈가 아니다. 여성도 나이가 들수록 머리카락이 가늘어지고 밀도가 줄어드는 것을 크게 체감한다.
- `14:09` 화자 7: `텔로미어`가 짧아질수록 노화가 시작되고 가속될 수 있다는 점이 인상적이었다.
- `14:29` 화자 1: 세포가 분열할 수 있는 횟수에 한계가 있다는 `Hayflick limit`는 매우 유명한 설명이다. 이런 논의는 노화를 외형이 아니라 세포 수준에서 보게 만든다.
- `15:31` 화자 1: 우리는 흔히 피부, 주름, 머리카락처럼 눈에 보이는 변화만 생각하지만, 생물학적 노화 이론은 세포와 유전자 수준의 미시적 손상과 조절 실패를 다룬다.
- `15:59` 화자 8: 이 장의 핵심은 단지 늙어 보이는 모습보다 `기능`과 `자립성`의 변화를 설명하는 데 있다고 읽었다.
- `16:33` 화자 1: 맞다. 이 장의 여러 내용은 결국 신체 변화가 기능과 자립에 어떤 차이를 만드는지를 설명하는 쪽으로 수렴한다.
- `17:10` 화자 9: 노인들은 단순히 잠을 적게 자는 게 아니라 수면 패턴 자체가 다르게 나타난다는 점이 인상적이었다. `REM 수면`이 반복되고 얕아져 쉽게 깬다는 부분이 새로웠다.
- `19:49` 화자 1: 불면을 호소하는 할머니 사례 영상을 보면, 실제로는 잠든 장면이 관찰되는데 본인은 거의 못 잤다고 느낀다.
- `25:51` 화자 1: 이것이 전형적인 노년기 수면 문제다. 잠이 전혀 없는 게 아니라 `수면의 질`이 얕고 쉽게 깨기 때문에 본인은 못 잤다고 느낀다.
- `29:12` 화자 1: 최근에는 수면제를 무조건 피하는 것보다, 잠을 못 자서 인지 건강이 더 악화되는 위험도 함께 보자는 논의가 많다. 잠 부족은 나중의 `Cognitive Aging`과도 연결될 수 있다.
- `30:42` 화자 10: 손상오류이론을 보면서 노화가 누적되다가 어느 순간 임계치를 넘으면 갑자기 확 드러나는 것처럼 느껴졌다.
- `31:16` 화자 1: 최근 기사에서는 사람이 `34세`, `60세`, `78세` 부근에서 급격한 노화 변화를 보인다고 소개하지만, 이런 자료는 어디까지나 외부 기사다.
- `34:38` 화자 1: 그 연구는 `18세~95세` 참가자 혈액을 한 번에 비교한 `횡단 연구`다. 그래서 변화처럼 보이는 차이를 곧바로 인과나 개인 내 변화로 읽으면 안 된다.
- `37:02` 화자 1: 생물학적 지표라서 횡단 비교가 심리사회적 변수보다 덜 공격받을 수는 있지만, 그래도 인과관계를 말할 수 있는 설계는 아니다.
- `39:46` 화자 1: 그래서 핵심은 다시 `1차적`, `2차적`, `3차적 노화`로 돌아온다. 많은 개입은 `2차적 노화`를 줄이는 방향에 몰려 있다. 운동, 식이, 생활습관 교정처럼 막을 수 있는 부분이 있기 때문이다.
- `40:46` 화자 1: 나이를 출생으로부터의 거리로 볼 수도 있지만, 반대로 `죽음으로부터의 거리(time to death / distance to death)`로 다시 정렬해 보면 다른 패턴이 보인다.
- `41:31` 화자 1: 그렇게 보면 돌아가시기 `2~3년 전`부터 `frailty` 점수는 급격히 올라가고 `life satisfaction`은 떨어지는 커브가 보일 수 있다.
- `42:31` 화자 1: 인지능력도 마찬가지로 생애 말기에 급격한 하강이 나타난다. 그래서 `terminal decline`은 단지 추상적 개념이 아니라 임상적으로도 자주 관찰되는 현상이다.
- `46:43` 화자 1: 교재는 비교적 고전적인 생물학적 노화 이론을 소개하지만, 최근 연구에서는 `Hallmarks of Aging` 프레임이 확장되었고 `disabled macroautophagy`, `dysbiosis`, `chronic inflammation` 같은 요소가 더 논의되고 있다.
- `49:29` 화자 1: 이런 연구를 하는 이유는 원인을 알면 `intervention`이 가능할지 모른다는 기대 때문이다. 줄기세포, 텔로미어, 후성유전 변화 등은 모두 개입 가능성과 연결되어 읽힌다.
- `54:33` 화자 1: `데이비드 싱클레어`는 노화를 질병처럼 보고 개입 가능하다고 말하는 대표적 인물이다. 다만 이런 프레임은 교재의 기본 구분보다 훨씬 급진적이다.
- `57:20` 화자 13: `Altos Labs` 같은 연구소에 막대한 자금이 몰리는 이유도 노화 치료 가능성에 대한 기대 때문이다. `제프 베조스`, `유리 밀너`, `야마나카 신야` 같은 이름들이 이 맥락에서 등장한다.
- `01:01:17` 화자 1: 만약 이런 논의를 끝까지 밀어붙이면, 우리가 처음 교재에서 배운 `1차적 노화` 자체의 의미도 다시 흔들릴 수 있다. 원래는 불가피한 변화로 봤던 것을 치료 대상으로 돌리려는 셈이기 때문이다.
- `01:08:04` 화자 1: 그런데 오래 사는 기술이 가능해지더라도 그 혜택이 공평하게 분배될지는 별개의 문제다.
- `01:08:25` 화자 19: 단기적으로는 그런 기술을 돈 있는 사람만 먼저 누리게 될 가능성이 크다.
- `01:10:20` 화자 3: 수명 연장이 사회적으로 준비되지 않은 상태에서 이루어지면, 개인에게 정말 유의미한 좋은 일인지 의문이 든다.
- `01:14:13` 화자 1: 인문사회 쪽에서는 장수 자체보다 돌봄, 불평등, 사회구조 문제를 같이 보게 된다.
- `01:15:12` 화자 1: 다음 시간에는 인간의 최대수명 논쟁과 관련된 글, 그리고 기술혁신 기사들을 읽고 이어서 논의한다.
